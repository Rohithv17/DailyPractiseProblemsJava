package com.bridgelabz.lastindex;

public class LastIndex {
    public void checkLastIndex(String s){
        int lastindex=s.length()-1;
        System.out.println(s.charAt(lastindex)); // prints character at last index of the string 
    }
}
